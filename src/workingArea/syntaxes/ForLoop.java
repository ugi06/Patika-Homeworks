package workingArea.syntaxes;

public class ForLoop {
    public static void main(String[] args) {
        /**
         * bir değişken tanımlayıp döngü ile değiştirebiliriz ama dışında tanımlanmalı
         */
        for (int i = 1; i < 65; i = i + 2) { // başlangıç koşulu; sonlanma koşulu; artış  birimi
            /**
             * kod satırı
             *i ikişer artarak 65 i geçene kadar her adımda kendini tekrarlar
             * break; ile döndügen atabilrsin
             * if-else, switch-case, do-while metotları kullanılabilir
             * öz yenilemeli yapılar için kullanışlı
             */
        }
    }
}
